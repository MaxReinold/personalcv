export default function Home() {


  return (
    <div className="absolute inset-2 flex items-center justify-center flex-col">
      <div className="text-5xl flex gap-3 items-center">{"<"}Maxwell Reinold <span className="text-3xl text-muted">inProgress={"{true}"}</span>{"/>"}</div>
    </div>
  );
}
